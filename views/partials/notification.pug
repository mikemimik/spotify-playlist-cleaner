//- inside body tag; above content
if error
  div(class='alert alert-warning', role='alert')
    button(type='button', class='close', data-dismiss='alert', aria-label='close')
      span(aria-hidden='true') &times;
    strong=error.message
